<IfModule mod_rewrite.c>
    RewriteEngine On

    # Block access to sensitive files
    <FilesMatch "^\.env|^\.gitignore|^\.git">
        Order allow,deny
        Deny from all
    </FilesMatch>

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
